{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "g++ Car_main.cpp Car.cpp -o Car && ./Car",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
